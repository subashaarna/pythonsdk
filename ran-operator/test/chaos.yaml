apiVersion: ranoperator.example.com/v1alpha1
kind: RanOperator
metadata:
  name: test-ran-operator
  namespace: ran-operator
spec:
  name: example-name
  test: testing
  namespace: example-namespace
  replicas: 3
  # Add more fields as needed
